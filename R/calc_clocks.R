# mc_provenance
# mc_groups
# mc_catalog |> lengths()
# mc_bundles |> lengths()
# mc_catalog$DNAmIC
#
# mc_catalog |> sapply(\(x) {x$computation_type})

calc_clocks <- function(DNAm, clocks, pheno = NULL, pheno_id = NULL, ...) {}
