% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc-params.R
\name{mc-params}
\alias{mc-params}
\title{Shared Parameters}
\arguments{
\item{DNAm}{A numeric matrix. The methylation beta values, with samples in
the rows and CpGs in the columns.}

\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}

\item{clocks}{A character vector. The clocks to score, named by clock id,
group id, or tag. See \code{\link[=list_clocks]{list_clocks()}}.}

\item{pheno}{A data.frame. The sample metadata, with one row for each
sample. Default is \code{NULL}.}

\item{covariates}{A named character vector. Points a covariate at the column
that holds it, for metadata that names its columns something else. Default
is \code{NULL}.}

\item{normalize}{A named logical vector. Turns background normalization on
or off for the clocks that declare a method. Default is \code{NULL}.}

\item{ext_data}{A string. The path to the directory that holds the clock
assets. Default is \code{NULL}, which uses the assets directory.}

\item{ask}{A boolean. Asks for confirmation before the assets directory
changes. Default is \code{TRUE}. Pass \code{FALSE} to continue without asking, in a
non-interactive session.}

\item{all_columns}{A boolean. Returns every column, including the ones the
frame leaves out by default. Default is \code{FALSE}.}

\item{groups}{A character vector. The asset groups to act on. One or more
of \code{"PCBrainAge"}, \code{"PCClocks"}, \code{"SystemsAge"} and \code{"Zhang2019"}, or
\code{"all"} for every group. Repeated values are ignored, and an empty vector
selects nothing. Default is \code{"all"}.}

\item{long}{A boolean. Returns one row for each sample and clock when
\code{TRUE}, and one row for each sample, with one column for each clock, when
\code{FALSE}. Default is \code{TRUE}.}
}
\description{
Parameter definitions reused across the package.
}
\section{The assets directory}{

Four clock groups keep their weights in downloadable assets, outside the
package. \code{ext_data} says where to read them from, and accepts three forms.
\itemize{
\item \code{NULL} reads from the assets directory, and downloads any asset that is
missing. Use \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose that directory.
\item A path reads only that directory, and never downloads. A missing asset
is an error.
\item Assets already in memory from \code{\link[=load_mc_assets]{load_mc_assets()}} are used directly.
}
}

\section{Normalization}{

Some clocks declare a background normalization method. \code{normalize} accepts
four forms.
\itemize{
\item \code{NULL} leaves every clock at its own default. A clock that declares
\code{quantile} is normalized, and a clock that declares \code{bmiq} is not.
\item \code{TRUE} or \code{FALSE} sets every clock that declares a method.
\item A character vector of clock ids turns normalization on for those clocks,
and leaves every other clock at its default.
\item A named logical vector sets the clocks it names, and leaves every other
clock at its default.
}

The character form only turns normalization on. To turn a method off, name
the clock in a named logical vector.

Normalization needs a background panel that is much larger than the scoring
panel, so a matrix cut down to the scoring CpGs cannot supply it. The
\code{normalize} column of \code{\link[=list_clocks]{list_clocks()}} with \code{all_columns = TRUE} gives the
method each clock declares.
}

\section{Covariate columns}{

Some clocks read a covariate from the sample metadata. A clock names the
covariate it reads, and looks for a column of that name.

\code{covariates} points a covariate at a column that holds it under another
name. Write the covariate on the left and your own column on the right, as
in \code{covariates = c(Age = "age_yrs")}. Only a covariate this call reads can
be pointed at a column, and each one may be pointed once.

A covariate that is already a column of the metadata needs no entry. The
\code{covariates} column of \code{\link[=list_clocks]{list_clocks()}} with \code{all_columns = TRUE} gives the
covariates each clock reads.

A pointer moves the name and nothing else. \code{Female} pointed at a column that
holds \code{1} for male scores every clock that reads sex, and scores all of them
wrong, so check what the column means before you point at it.
}

\section{Clocks that use all the samples}{

Some clocks depend on information from all the samples, such as a z-score.
When \code{x} holds more than one batch, these clocks take their value from
every sample in \code{x}, and not from one batch alone. This is the same
calculation as \code{\link[=refinalize_clocks]{refinalize_clocks()}}.
}

\keyword{internal}
