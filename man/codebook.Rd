% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{codebook}
\alias{codebook}
\alias{codebook.character}
\alias{codebook.mc_result}
\alias{codebook.default}
\title{Clock Codebook}
\usage{
codebook(x, ...)

\method{codebook}{character}(x, ...)

\method{codebook}{mc_result}(x, ...)

\method{codebook}{default}(x, ...)
}
\arguments{
\item{x}{A character vector. The clock ids, group ids or tags to describe,
or an \code{mc_result} object from \code{\link[=calc_clocks]{calc_clocks()}}.}

\item{...}{Not used.}
}
\value{
A data.frame. One row for each described clock, with the \code{column}
it appears as and a one sentence \code{description} of it.
}
\description{
Describes a set of clocks, or the clocks scored in an \code{mc_result} object.
}
\details{
A character vector describes the clocks it names. A group id describes every
clock in the group, and a tag describes every clock the tag expands to. See
\code{\link[=list_clocks]{list_clocks()}} and \code{\link[=list_clock_tags]{list_clock_tags()}}. An \code{mc_result} object describes the
clocks in its scores.

The first row reports the version of the clock descriptions. The second row
describes the \code{clock_id} column of \code{\link[=as.data.frame]{as.data.frame()}}. Each row after that is
one value of that column.

A clock with no description on record reads \code{NA}.
}
\examples{
codebook(c("Horvath1", "Hannum"))

sim <- sim_DNAm("Hannum", n = 5)
res <- calc_clocks(sim[["DNAm"]], "Hannum")
codebook(res)

}
