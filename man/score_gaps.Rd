% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_gaps.R
\name{score_gaps}
\alias{score_gaps}
\title{Reasons A Score Is Missing}
\usage{
score_gaps(x)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}
}
\value{
A data.frame. One row for each missing score, with the sample
\code{id}, the \code{clock_id}, the \code{reason}, and, when \code{x} holds more than one
batch, \code{mc_batch_id}.
}
\description{
Reports why each \code{NA} score in \code{x} is missing, one row for each missing
score.
}
\details{
Every reason is worked out from \code{x} itself, so the frame always agrees
with the coverage counts and the gates that \code{calc_clocks()} used. A run
with no missing score gives a frame with no rows.

The \code{reason} column takes one of five values. Where more than one applies,
the first of these is given.
\itemize{
\item \code{covariate}, when a covariate the clock needs is missing from \code{pheno}.
An unknown \code{Female} value is the usual cause.
\item \code{clock_coverage}, when the clock is under \code{min_clocks_coverage}. Every
sample is missing this score.
\item \code{sample_coverage}, when the sample is under \code{min_samples_coverage}.
\item \code{fit}, when the clock reached the sample but could not be calculated
for it.
\item \code{dependency}, when a clock that this clock is calculated from is
missing for that sample.
}

A \code{dependency} row always names a clock that has its own rows in the
frame, so the cause can be followed to it. A clock scored separately for
each sex takes the reason of the model for that sample's sex, because
that model is not one of the scores of \code{x}.

Each floor is read for the batch the sample was scored in, which is the
floor that decided the score. The \code{mc_batch_id} column appears only when
\code{x} holds more than one batch.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 20)
# keep half the CpGs, so a clock falls under the coverage floor
DNAm <- sim[["DNAm"]]
res <- calc_clocks(DNAm[, seq(1, ncol(DNAm), by = 2)], clocks)

head(score_gaps(res))

}
