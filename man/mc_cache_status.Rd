% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methylCIPHER_data.R
\name{mc_cache_status}
\alias{mc_cache_status}
\title{Status of the cached external clock packs}
\usage{
mc_cache_status(groups = "all", path = NULL)
}
\arguments{
\item{groups}{Character vector of group ids, or \code{"all"} (default) for every external
group.}

\item{path}{Cache directory. \code{NULL} (default) uses \code{\link[=mc_cache_dir]{mc_cache_dir()}}.}
}
\value{
A \code{data.frame} with one row per group: \code{group_id}, \code{file}, \code{cached} (logical),
\code{size_bytes} (expected download size), \code{n_clocks}, and \code{path} (where the file is or
would be).
}
\description{
Reports, for each external clock group, the exact file this package version expects and
whether it is present in the cache. Purely a query: it never downloads, deletes, or
creates anything.
}
\examples{
mc_cache_status()
}
\seealso{
\code{\link[=mc_data_download]{mc_data_download()}}, \code{\link[=mc_data_clear]{mc_data_clear()}}
}
