% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{print.mc_summary}
\alias{print.mc_summary}
\title{Print Method For An mc_summary Object}
\usage{
\method{print}{mc_summary}(x, n = 6, ...)
}
\arguments{
\item{x}{An \code{mc_summary} object.}

\item{n}{A single whole number. The number of rows to print for each
table. Default is \code{6}.}

\item{...}{Not used.}
}
\value{
An \code{mc_summary} object. Returns \code{x}, invisibly, after printing it.
}
\description{
Prints the tables of an \code{mc_summary} object.
}
\details{
The two problem tables print the \code{note} of each row. The notes table under
them gives the \code{explanation} of every note they print. The \code{explanation}
stays in \code{x} beside the \code{note}.

Every table shortens \code{mc_batch_id} to its first seven characters, except
the \code{mc_batch_id} table, which gives every label in full.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 20)
res <- calc_clocks(sim[["DNAm"]], clocks)
print(summary(res), n = 3)

}
