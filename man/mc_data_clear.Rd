% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methylCIPHER_data.R
\name{mc_data_clear}
\alias{mc_data_clear}
\title{Remove cached external clock data}
\usage{
mc_data_clear(groups = "all", path = NULL, ask = TRUE)
}
\arguments{
\item{groups}{Character vector of group ids to clear, or \code{"all"} (default).}

\item{path}{Cache directory. \code{NULL} (default) uses \code{\link[=mc_cache_dir]{mc_cache_dir()}}.}

\item{ask}{Ask for confirmation before deleting? Defaults to \code{TRUE}. In a
non-interactive session this errors instead of prompting -- pass \code{ask = FALSE} to
consent explicitly.}
}
\value{
Invisibly, a character vector of the deleted file paths.
}
\description{
Deletes the downloaded clock packs from the cache. This is the counterpart to
\code{\link[=mc_data_download]{mc_data_download()}}: nothing methylCIPHER writes to disk survives it.
}
\details{
Files from older package versions are removed too. Because pack filenames carry a
content hash, upgrading methylCIPHER leaves the previous pack behind as dead weight;
clearing a group removes every \verb{<group>-*.qs2} in the cache, not only the file the
current version expects.
}
\examples{
\dontrun{
mc_data_clear("PCClocks")
mc_data_clear(ask = FALSE)
}
}
\seealso{
\code{\link[=mc_data_download]{mc_data_download()}}, \code{\link[=mc_cache_status]{mc_cache_status()}}
}
