% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methylCIPHER_data.R
\name{mc_data_download}
\alias{mc_data_download}
\title{Download the external clock data packs}
\usage{
mc_data_download(
  groups = "all",
  path = NULL,
  force = FALSE,
  ask = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{groups}{Character vector of group ids to download, or \code{"all"} (default). See
\code{\link[=mc_cache_status]{mc_cache_status()}} for the available groups.}

\item{path}{Destination directory. \code{NULL} (default) uses \code{\link[=mc_cache_dir]{mc_cache_dir()}}.}

\item{force}{Re-download packs that are already cached? Defaults to \code{FALSE}.}

\item{ask}{Ask for confirmation before writing to disk? Defaults to \code{TRUE}. In a
non-interactive session this errors instead of prompting -- pass \code{ask = FALSE} to
consent explicitly from a script.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, a named character vector of cached file paths (one per requested
group).
}
\description{
The SystemsAge, PCClocks, and PCBrainAge clocks depend on weight matrices far too large
to ship inside a CRAN package, so they are published as release assets and downloaded
once into a local cache. Everything else in methylCIPHER works without this.
}
\details{
Files are content-addressed: the name carries a hash of the pack contents, and every
download is verified against the SHA-256 recorded in the package. A file that is already
present is left alone unless \code{force = TRUE}.
}
\examples{
\dontrun{
# everything, with a confirmation prompt
mc_data_download()

# one group, no prompt (scripts / CI)
mc_data_download("SystemsAge", ask = FALSE)
}
}
\seealso{
\code{\link[=mc_cache_status]{mc_cache_status()}}, \code{\link[=mc_data_clear]{mc_data_clear()}}, \code{\link[=mc_cache_dir]{mc_cache_dir()}}
}
