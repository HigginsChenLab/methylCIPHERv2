% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_clocks.R
\name{resolve_clocks}
\alias{resolve_clocks}
\title{Resolve user clock tokens to catalog \code{clock_id}s}
\usage{
resolve_clocks(clocks)
}
\arguments{
\item{clocks}{Character vector of aliases, group ids, and/or clock ids.
Must be non-empty. \code{NULL} / missing is an error (callers that want
everything should pass \code{"all"}).}
}
\value{
Character vector of unique catalog \code{clock_id}s, in first-seen
order after expansion.
}
\description{
Maps a character vector of user requests to a de-duplicated vector of
catalog \code{clock_id}s. Accepts:
\itemize{
\item alias names registered in \code{.CLOCK_ALIASES} (currently \code{"all"})
\item \code{group_id} — expanded to every clock in that group
\item \code{clock_id} — kept as a single id
}
}
\examples{
resolve_clocks("Hannum")
resolve_clocks("Bohlin")                 # → Bohlin251, Bohlin96
resolve_clocks(c("Bohlin", "Bohlin251")) # unique → both members once
resolve_clocks(c("Hannum", "all"))       # all clocks (Hannum first, rest after)

}
\keyword{internal}
